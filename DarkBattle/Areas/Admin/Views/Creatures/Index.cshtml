@using DarkBattle.Services.ServiceModels.Creatures
@model IEnumerable<CreatureServiceListModel>

@{
    ViewData["Title"] = "Creatures";
}

<h1>Creatures</h1>

<p>
    <a asp-area="Admin" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Level)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Area)
            </th>
            <th class="text-centre">Options</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Level)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Area)
            </td>
            <td>
                <a asp-area="Admin" asp-action="Create" asp-route-creatureId="@item.Id">Copy</a> |
                <a asp-area="Admin" asp-action="Edit" asp-route-creatureId="@item.Id">Edit</a> |
                <a asp-area="Admin" asp-action="Delete" asp-route-creatureId="@item.Id">Delete</a> |
                <a asp-area="Admin" asp-controller="CreatureItems" asp-action="CreatureItems" asp-route-creatureId="@item.Id">Items</a> |
                <a asp-area="Admin" asp-controller="CreatureConsumables" asp-action="CreatureConsumables" asp-route-creatureId="@item.Id">Consumables</a>
            </td>
        </tr>
}
    </tbody>
</table>
